export const RECIPIENT_MESSAGES = {
  duplicateEmail: 'Recipient email already exists',
  notFound: 'Recipient not found'
} as const

export const RECIPIENT_PAGINATION = {
  defaultLimit: 8,
  maxLimit: 20,
  minPage: 1
} as const
